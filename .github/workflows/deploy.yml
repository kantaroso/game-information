name: deploy
on:
  workflow_dispatch:
    inputs:
      backend:
        description: 'backend release'
        default: false
        type: boolean
      frontend:
        description: 'frontend release'
        required: false
        type: boolean
jobs:
  deploy-backend:
    if: ${{ inputs.backend }}
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{ secrets.RENDER_DEPLOY_URL_PROD }}
  deploy-frontend:
    if: ${{ !failure() && inputs.frontend }}
    needs: deploy-backend
    runs-on: ubuntu-latest
    steps:
      - run: echo "deploy-frontend"
